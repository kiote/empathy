package sensors

import (
	"fmt"
	// "net/http"
)

// var (
// 	serverUrl = "http://localhost"
// 	serverPort = "22002"
// )

func StartExperiment() {
	fmt.Printf("GSR Sensor initializing...")
	// resp, err := http.Get(serverUrl + ":" + serverPort + "/NeuLogAPI?GetServerVersion")
	// fmt.Printf("%v", resp)
	// fmt.Printf("%e", err)
}

